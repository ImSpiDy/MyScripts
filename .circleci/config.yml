version: 2.1
jobs:
  compile:
   docker:
      - image: z4nyx/docker:latest
   steps:
      - run:
          command: |
           git config --global user.name "ImSpiDy"
           git config --global user.email "SpiDy2713@gmail.com"
           git config --global color.ui false
           git clone --depth=1 https://github.com/ImSpiDy/kernel_xiaomi_lavender -b staging .
           bash build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
