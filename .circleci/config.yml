version: 2.1
jobs:
  snx:
   docker:
      - image: nexgang/ubuntu:latest
   steps:
      - run:
          command: |
           git config --global user.name "ImSpiDy"
           git config --global user.email "SpiDy2713@gmail.com"
           git config --global color.ui false
           git clone --depth=1 https://ImSpiDy:$ght@github.com/ImSpiDy/kernel_xiaomi_lavender-4.4 -b snx-rdp-v .
           bash build.sh --rdp
workflows:
  version: 2.1
  cooking:
    jobs:
      - snx
